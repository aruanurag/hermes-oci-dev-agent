model:
  provider: custom
  base_url: "http://127.0.0.1:8181/v1"
  api_key: "not-used"
  default: "${genai_model_id}"
  context_length: 131072

terminal:
  backend: local
  cwd: /home/hermes/workspace
  timeout: 60

# Hermes runs as the dedicated, non-root `hermes` OS user. No sudo is granted.
agent:
  toolsets:
    - file
    - terminal
